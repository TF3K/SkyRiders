import { PayoutForm } from "@/components/shop/payout-form";

export default function Payout(){
    return (
        <div className="h-screen flex items-center justify-center">
            <PayoutForm />
        </div> 
    )
}